var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  Options: () => import_related_documents2.Options,
  related: () => related
});
module.exports = __toCommonJS(src_exports);

// src/related.ts
var import_related_documents = require("related-documents");
var related = (options) => {
  let related2;
  return (document, documents) => {
    if (!related2 || !shallowIsEqual(documents, related2.documents)) {
      related2 = new import_related_documents.Related(documents, options);
    }
    return related2.rank(document);
  };
};
var shallowIsEqual = (a, b) => {
  if (a.length !== b.length) {
    return false;
  }
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) {
      return false;
    }
  }
  return true;
};

// src/index.ts
var import_related_documents2 = require("related-documents");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  Options,
  related
});
//# sourceMappingURL=index.js.map
